fun longestSubstringWithDistinctCharacters(string: String, k: Int): Int{
    val charactersUsing = hashMapOf<Char, Int>()
    var startChar: Char
    var maxLength: Int
    var currentLength: Int
    for (endChar in string.indices){
        if (!charactersUsing.containsKey(string[endChar])) {

        }
    }
}